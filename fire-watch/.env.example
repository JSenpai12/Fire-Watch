# Base URL for the Fire Watch FastAPI backend.
# In dev, leave unset to use the Vite proxy at /api (see vite.config.ts),
# which forwards to http://localhost:8000.
# In production, point this at your deployed backend, e.g.:
# VITE_API_BASE_URL=https://api.firewatch.example.com
VITE_API_BASE_URL=
